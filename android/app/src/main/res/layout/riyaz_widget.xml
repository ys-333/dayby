<?xml version="1.0" encoding="utf-8"?>
<!--
  Deliberately plain: states are carried by text glyphs (checkmark, ring,
  cross) rather than hue, because a widget is drawn at whatever size and over
  whatever wallpaper the user chose.

  Colours are named app colours with a `-night` variant, not `?android:attr`
  theme attributes. A widget is inflated by the launcher, so a theme attribute
  resolves against the launcher's theme rather than the user's dark-mode
  setting; see values/colors.xml for what that looked like in practice.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="12dp"
    android:background="@color/widget_surface">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:paddingBottom="6dp">

        <TextView
            android:id="@+id/date_label"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:textSize="13sp"
            android:textStyle="bold"
            android:maxLines="1"
            android:ellipsize="end"
            android:textColor="@color/widget_ink" />

        <TextView
            android:id="@+id/progress_label"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="13sp"
            android:textStyle="bold"
            android:textColor="@color/widget_ink_dim" />
    </LinearLayout>

    <TextView android:id="@+id/row_0" style="@style/RiyazWidgetRow" />
    <TextView android:id="@+id/row_1" style="@style/RiyazWidgetRow" />
    <TextView android:id="@+id/row_2" style="@style/RiyazWidgetRow" />
    <TextView android:id="@+id/row_3" style="@style/RiyazWidgetRow" />
    <TextView android:id="@+id/row_4" style="@style/RiyazWidgetRow" />
</LinearLayout>
