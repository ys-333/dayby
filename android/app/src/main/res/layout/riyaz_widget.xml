<?xml version="1.0" encoding="utf-8"?>
<!--
  Deliberately plain: a widget is drawn by the launcher in a theme this app
  does not control, so it uses system attributes rather than app colours and
  states are carried by text glyphs (checkmark, ring, cross) rather than hue.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="12dp"
    android:background="?android:attr/colorBackground">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:paddingBottom="6dp">

        <TextView
            android:id="@+id/date_label"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:textSize="13sp"
            android:textStyle="bold"
            android:maxLines="1"
            android:ellipsize="end"
            android:textColor="?android:attr/textColorPrimary" />

        <TextView
            android:id="@+id/progress_label"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="13sp"
            android:textStyle="bold"
            android:textColor="?android:attr/textColorSecondary" />
    </LinearLayout>

    <TextView android:id="@+id/row_0" style="@style/RiyazWidgetRow" />
    <TextView android:id="@+id/row_1" style="@style/RiyazWidgetRow" />
    <TextView android:id="@+id/row_2" style="@style/RiyazWidgetRow" />
    <TextView android:id="@+id/row_3" style="@style/RiyazWidgetRow" />
    <TextView android:id="@+id/row_4" style="@style/RiyazWidgetRow" />
</LinearLayout>
